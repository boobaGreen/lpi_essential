import { quizzes_it, allQuizzes_it } from './it.js'

export const itilQuizzesDict = {
  it: quizzes_it,
  en: quizzes_it // Fallback to IT for now
}

export const itilAllQuizzesDict = {
  it: allQuizzes_it,
  en: allQuizzes_it // Fallback to IT for now
}
