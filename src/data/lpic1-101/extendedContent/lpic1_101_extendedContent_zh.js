// LPIC-1 101 Extended Content — Chinese

export const lpic1_101_extendedContent_zh = {
  // ─── TOPIC 1 ───
  'lpic1-101-1-1': `
### \`lspci\` 命令详解

\`lspci\` 命令直接从内核中提取信息（通过 sysfs 和 /proc/bus/pci）。如果不带选项，它会提供一个快速摘要。通过 \`-v\`、\`-vv\` 或 \`-vvv\`，详细程度会增加，重点关注：

*   **IRQ (中断请求)**：硬件用于引起 CPU 注意的中断线。
*   **I/O 和内存地址**：设备如何将其内存在主 RAM 中映射。
*   **正在使用的模块**：当前控制该卡的内核驱动程序（例如：\`Kernel driver in use: e1000e\`）。

\`-k\` 参数是 LPIC-1 中的最爱，因为它准确显示了硬件支持哪个模块（\`Kernel modules: ...\`）以及当前正在使用哪个模块，在故障排除中非常有用！
  `,
  'lpic1-101-1-2': `
### GRUB 中隐藏的内核参数

系统启动时，GRUB 会将“选项”（命令行参数）传递给 Linux 内核。这些可以通过检查 \`cat /proc/cmdline\` 来查看。常用参数包括：

*   \`ro\`：根文件系统最初以只读 (Read-Only) 方式挂载，以确保启动时的安全，然后再重新挂载为读写 (rw)。
*   \`quiet\`：禁止控制台上的友好硬件消息（所以之后需要使用 \`dmesg\`！）。
*   \`nomodeset\`：告诉内核在完整的窗口管理器 (X11/Wayland) 启动之前不要尝试加载高分辨率视频驱动程序。如果在安装后旧的 NVIDIA 显卡出现黑屏，这是至关重要的。
  `,

  // ─── TOPIC 2 ───
  'lpic1-102-1-1': `
### MBR vs GPT：结构限制

**MBR (主引导记录)**：
*   将其路由表写入硬盘的微小第 0 扇区（512 字节）。
*   只能拥有 **4 个主分区**。
*   为了突破这个限制，发明了“扩展分区”（一个消耗 4 个主分区之一的虚拟“盒子”），其中可以存在多个“逻辑分区”。

**GPT (GUID 分区表)**：
*   是基于 UEFI 的，且规模庞大。
*   原生支持 **128 个主分区**。
*   消除了 MBR 建立的每分区“2 TB”的限制。
*   在硬盘的确切末尾保留一份隐藏的分区表备份！
  `,

  // ─── TOPIC 4 ───
  'lpic1-104-2-1': `
### /etc/fstab 中的 UUID

建议使用 **UUID** (通用唯一识别码) 而不是设备名称 (\`/dev/sda1\`)。设备名称可能会根据电缆或硬盘重新连接而变化，而 UUID 对于格式化的分区是保持不变的。可以使用 \`blkid\` 命令查找 UUID。
  `,
};
