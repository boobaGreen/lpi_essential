// LPIC-1 101 Extended Content — Russian

export const lpic1_101_extendedContent_ru = {
  // ─── TOPIC 1 ───
  'lpic1-101-1-1': `
### Анатомия команды \`lspci\`

Команда \`lspci\` извлекает информацию напрямую из ядра (через sysfs и /proc/bus/pci). Без параметров она дает краткую сводку. С \`-v\`, \`-vv\` или \`-vvv\` детализация увеличивается:

*   **IRQ (Interrupt Requests)**: Линии прерываний, используемые оборудованием для привлечения внимания процессора.
*   **Адреса I/O и памяти**: Как устройство отображает свою память в основную ОЗУ.
*   **Используемые модули**: Какой драйвер ядра управляет картой в данный момент (напр., \`Kernel driver in use: e1000e\`).

Параметр \`-k\` полезен для LPIC-1, так как показывает, какие модули поддерживает оборудование и какой используется сейчас.
  `,
  'lpic1-101-1-2': `
### Параметры ядра в GRUB

При запуске системы GRUB передает ядру "опции" (параметры командной строки). Их можно увидеть с помощью \`cat /proc/cmdline\`.

*   \`ro\`: Корневая файловая система монтируется только для чтения (Read-Only) для безопасности во время загрузки.
*   \`quiet\`: Скрывает сообщения о загрузке (для этого есть \`dmesg\`!).
*   \`nomodeset\`: Указывает ядру не загружать драйверы высокого разрешения до старта графической оболочки. Помогает при проблемах с видеокартами.
  `,

  // ─── TOPIC 2 ───
  'lpic1-102-1-1': `
### MBR против GPT

**MBR (Master Boot Record)**:
*   Хранится в секторе 0 (512 байт).
*   Только **4 основных раздела**.
*   Лимит 2 ТБ.

**GPT (GUID Partition Table)**:
*   Основан на UEFI.
*   До **128 основных разделов**.
*   Поддержка огромных дисков.
*   Хранит резервную копию таблицы в конце диска.
  `,

  // ─── TOPIC 4 ───
  'lpic1-104-2-1': `
### UUID в /etc/fstab

Рекомендуется использовать **UUID** (Universal Unique ID) вместо имен устройств (\`/dev/sda1\`). Имена устройств могут меняться при переподключении кабелей или дисков, а UUID остается неизменным для отформатированного раздела. Узнать UUID можно командой \`blkid\`.
  `,
};
